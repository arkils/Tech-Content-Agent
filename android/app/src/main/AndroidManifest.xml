<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml
    ====================
    Declares the TechContentAgent app components.

    Required permissions:
      - POST_NOTIFICATIONS  (Android 13+) — show FCM notifications
      - INTERNET            — Lambda Function URL API calls

    Add google-services.json from the Firebase Console before building.
    See docs/hitl-plan.md for setup instructions.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechContentAgent">

        <!-- Main entry point -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep-link from FCM notification tap -->
            <intent-filter>
                <action android:name="com.techcontent.agent.OPEN_APPROVAL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- FCM message receiver -->
        <service
            android:name=".fcm.TechAgentMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
